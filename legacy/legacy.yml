---
- vars_files:
    - vars.yml
  hosts:
    - localhost
  remote_user: "{{ host_user }}"
  roles:
    - minimal
    - mongodb
    - node
    - mysql
    - php
    - python

# For multiple playbooks uncomment the following section
# - vars_files:
#     - vars.yml
#   hosts:
#     - localhost
#   remote_user: "{{ host_user }}"
#   roles:
#     - minimal
#     - mongodb
#     - node
#     - mysql
#     - php
#     - python
